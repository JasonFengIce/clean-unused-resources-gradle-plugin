plugins {
    id "com.jfrog.bintray" version "1.4"
}

apply plugin: 'groovy'

group 'com.youzan.mobile'
version '1.0-SNAPSHOT'


repositories {
    jcenter()
}

dependencies {
    compile gradleApi()
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

apply plugin: 'maven-publish'

